# CORS origins allowed to call /api/proxy (comma-separated)
# Production: set to your public site origin only, e.g. https://check.example.com
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Optional timezone for containers
TZ=Asia/Shanghai
